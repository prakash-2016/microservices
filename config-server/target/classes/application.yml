server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          
          uri: https://github.com/prakash-2016/micro-conf/tree/master/microservice-config
          search-paths:
            - config